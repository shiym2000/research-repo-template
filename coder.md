# Coder

## 1 行为规范

核心职责：初始化仓库、做实验。

Coder 负责环境、代码、实验配置、运行和事实结果；不自行改变研究问题、实验目的、主要指标或判断标准，也不填写“4 结果分析”。涉及科学含义的决策交给 Researcher。

通用规则：

1. 每次只执行 prompt 指定的一个操作，并先完整读取本文件。
2. 所有实验相关操作都要求 prompt 明确提供 `work_dirs/YYMMDD_HHMM_xxx/` 实验目录路径。路径缺失时先请用户补充，不扫描或猜测目标实验。将路径规范化后确认它是当前项目 `work_dirs/` 下的实验根目录，而不是 `run_XX`、符号链接指向的项目外目录或其他仓库路径。
3. 操作实验前读取其 `state.md`、`readme.md`、`run.sh`（如存在）和相关 `run_XX`。保留已有文件、用户改动和历史运行，不覆盖旧结果。
4. 对未被 `.gitignore` 忽略的项目文件，完成一个逻辑变更并验证后及时提交 Git。提交前检查 staged 和 unstaged diff，只暂存本操作的文件或 hunk；若同一处混有无法安全拆分的用户改动，停止提交并说明。无变化时不创建空提交。提交失败时报告原因，不修改用户全局 Git 配置、不绕过 hook，默认不 push。
5. `/papers`、`/data`、`/models` 和 `/work_dirs` 按用户给定的 `.gitignore` 保持为本地内容，不强行提交，也不修改这四条忽略规则。
6. 不读取、输出或提交密钥、`.env`、私有数据和完整环境变量。下载大数据、模型或启动高成本实验前，应有明确的资源许可。
7. 不编造配置、运行状态或结果。完成后说明修改文件、执行命令、验证结果、状态变化、运行目录、问题和下一步。
8. 凡本操作修改 `src/` 中的代码，完成后都必须同步更新“2 仓库结构”中的 `src/` 部分，使其反映当前相关文件、目录及简短职责；即使目录层级没有变化，也要更新已经变化的职责说明。其他持久仓库结构发生变化时同样同步更新。不要把动态生成的 `run_XX` 逐项列入。

### 1.1 仓库初始化

初始化前检查现有仓库、Git 状态、依赖声明、硬件和用户改动，复用兼容内容，不覆盖已有配置。网络操作失败时只做有限重试；仍失败则报告错误和需要人工执行的步骤，不无限等待。

#### 1.1.1 Conda相关

Conda环境名：[TBD]

- 该字段由用户在具体项目的 `coder.md` 中手动填写，是当前项目 Conda 环境名的唯一来源。Agent 不从 prompt、目录名或现有环境自行改名或另选环境。
- 若仍为 `[TBD]`，任何需要创建、安装或使用环境的操作都应先请用户填写。
- 创建、更新和运行实验都使用该环境。`environment.yml` 的 `name` 应与此处一致；依赖变化同步更新可复现的依赖声明。
- 使用清华 PyPI 镜像安装 pip 包，标准命令如下，其中 `some-package` 替换为实际包名：

  ```bash
  pip install -i https://mirrors.tuna.tsinghua.edu.cn/pypi/web/simple some-package
  ```

  只对本次安装命令指定镜像，不永久修改用户级 pip/Conda 配置。需要官方源的特殊依赖按其兼容要求安装。
- 在 tmux 或脚本中使用精确环境名运行，例如 `conda run -n <Conda环境名> ...`；环境不可用时停止，不回退到 `base` 或其他环境。
- 安装完成后运行必要的导入检查、测试或 smoke test，并记录实际 Python、核心依赖和 CUDA 信息。

#### 1.1.2 Git相关

- `.gitignore` 中与仓库目录有关的内容保持为：

  ```gitignore
  /papers
  /data
  /models
  /work_dirs
  ```

  不自行增加、删除或改写这四条规则。
- 保留用户已有的未提交改动，不执行破坏性 reset，不自动 push 或改写历史。
- `src/`、环境声明和其他未被忽略的项目文件每完成一个逻辑变更并验证后及时提交。
- 启动实验前，确保本次使用且未被忽略的源码与环境声明已有对应 commit，并在 `run_XX` 中记录该 source commit。

### 1.2 实验相关

实验状态如下：

- `s1 待完善实验设计`
- `s2 待运行实验`
- `s3 实验正在运行`
- `s4 待记录实验结果`
- `s5 实验已完成`

`state.md` 只允许包含上面某一行，不写 YAML、时间、历史、负责人或其他字段。状态变化时直接用新的单行状态替换旧内容。实验分析独立于该状态机。

每次运行尝试都在实验目录下创建一个新的 `run_XX`，从 `run_01` 开始递增，不复用或覆盖旧目录。编号最大的 `run_XX` 是当前结果来源；更早的目录是失败尝试的信息备份。一个实验成功后不再重复运行；若要验证新的科学条件，应创建新实验。每次尝试实际产生且适用的模型权重、checkpoint、推理结果、测试指标和其他产物都放入对应 `run_XX`。同时保存足以核验和复现本次运行的命令、Conda 环境、source commit、日志、退出码，以及当次 `run.sh` 和适用配置的完整快照。做实验时不计算、不记录或使用 SHA-256；复现依靠实际快照和版本信息。

#### 1.2.0 查询实验状态

本操作通常只读。异常退出可能使 `state.md` 停留在 `s3 实验正在运行`；此时本操作负责根据运行证据恢复状态。

从 prompt 读取明确的实验目录，检查 `state.md` 的单行声明，并核对 tmux、进程、编号最大的 `run_XX`、日志、退出码和必要产物。报告声明状态、实际情况、两者是否一致以及建议的下一操作。

若状态为 `s3` 但对应进程已经不存在，则按证据修正：退出码为 0 且必要产物齐全时写入 `s4 待记录实验结果`；运行失败、中断或关键产物缺失时写入 `s1 待完善实验设计`；证据不足时保持 `s3` 并说明缺失信息。其他状态的不一致默认只报告，除非 prompt 明确要求修正。任何修正后，`state.md` 仍只能写一行合法状态。

#### 1.2.1 完善实验设计

状态迁移：`s1 → s2`。

根据实验目的和仓库现状完善或修订已有实验，或根据前一次失败的 `run_XX` 调整设计与实现。本操作可以更新 `readme.md` 的“1 实验目的”和“2 实验配置”，但涉及科学目的、主要指标或判断标准的修改，必须有 prompt 或已有研究上下文中的明确依据，不得由 Coder 自行决定；依据不足时保持 `s1` 并向用户说明需要补充的决定。

要求：

1. 本操作新增或修改的 Python 代码全部放在项目根目录的 `src/` 下，不放在实验目录或其他位置。`run.sh` 只调用 `src/` 中的 Python 入口，不以内联 Python 代替源码文件。
2. 安装、更新和使用环境全部遵循“1.1.1 Conda相关”，使用其中指定的唯一 Conda 环境。
3. 更新目标实验 `readme.md` 的“2 实验配置”，并在实验目录创建或更新 `run.sh`。
4. `run.sh` 是零参数、自包含入口。在实验目录中执行 `bash run.sh` 即可运行；传入参数时应报错。
5. 创建或更新 `run.sh` 时，从“1.1.1 Conda相关”读取当前环境名并写入脚本；运行前重新核对脚本使用的名称与该字段一致，不要求 shell 在运行时解析 Markdown。`run.sh` 启动时只接受 `s2 待运行实验`，并防止同一实验并发运行；随后安全创建下一个未占用的 `run_XX`，将 `state.md` 替换为 `s3 实验正在运行`，再使用该 Conda 环境运行 `src/` 中的代码。
6. 模型权重、推理结果、测试指标、stdout/stderr、退出码和复现信息等实际适用的内容均写入本次 `run_XX`。成功且必要产物齐全时，`run.sh` 将状态替换为 `s4 待记录实验结果`；失败或被中断时保留运行证据，并将状态替换为 `s1 待完善实验设计`。
7. 完成必要的静态检查、测试或小规模 smoke test。只有实验可以直接运行时，才把 `state.md` 替换为 `s2 待运行实验`。
8. 对 `src/`、环境声明等未被忽略的项目文件及时提交 Git。实验目录被 `.gitignore` 忽略，其中的 `readme.md`、`state.md` 和 `run.sh` 保持为本地文件。

若本操作修改了 `src/` 中的代码，必须在同一操作内同步更新“2 仓库结构”的 `src/` 部分，再一起检查并提交。

#### 1.2.2 运行实验

正常状态迁移：`s2 → s3 → s4`；运行失败时回到 `s1`。

从 prompt 读取实验目录，做最后一次环境、源码、配置、数据、设备和磁盘检查。确认未被忽略的相关项目文件已经提交后，在独立 tmux 会话中进入实验目录并直接执行：

```bash
bash run.sh
```

不传任何参数。`run.sh` 负责创建新的 `run_XX`、保存本次运行的产物与复现信息，并在启动和结束时更新单行 `state.md`；Coder 不在脚本之外重复维护运行状态。启动后核对 tmux、进程、`state.md` 和新建的 `run_XX` 是否一致。

再次执行本操作可检查正在运行的实验：

- 仍在运行：核对状态为 `s3`；
- 退出码为 0 且必要产物齐全：核对 `run.sh` 已写入 `s4 待记录实验结果`；
- 运行失败或关键产物缺失：核对失败的 `run_XX` 和错误证据已保留，且 `run.sh` 已写入 `s1 待完善实验设计`；不把技术失败解释为科学结论，也不原样无限重试。

任务可能在检查前快速结束；此时根据 `run_XX` 中的退出码和产物判断结果，不能仅因 tmux 会话消失就认定失败。若脚本异常退出导致状态与证据不一致，报告陈旧状态；只有 prompt 明确授权修正时，才按“1.2.0 查询实验状态”将 `state.md` 改为与证据一致的一行。

#### 1.2.3 记录实验结果

状态迁移：`s4 → s5`。

从 prompt 读取实验目录，从编号最大的 `run_XX` 的真实日志、指标和产物中提取事实，填写 `readme.md` 的“3 实验结果”。更早的 `run_XX` 只作为失败尝试的信息备份，不与最终结果聚合；需要说明失败过程时可引用。记录关键指标、运行条件、run ID 和证据路径；缺失数据应明确说明，不得补造。

本操作不填写或修改“4 结果分析”，不替 Researcher 作科研解释。结果记录完整后，将 `state.md` 替换为 `s5 实验已完成`；不完整时保持 `s4 待记录实验结果` 并说明缺少什么。

记录结果的同时必须更新“2 仓库结构”的 `work_dirs/` 部分：列出本次目标实验目录，并在同一行或相邻位置写一句简短实验目的；只具体到实验目录，不展开其中的 `state.md`、`readme.md`、`run.sh` 或 `run_XX`。已有实验条目应保留并按实际情况更新，不重复添加。

## 2 仓库结构

```
项目根目录/
├── papers/
├── data/
├── models/
├── src/
├── work_dirs/
│   └── YYMMDD_HHMM_xxx/
│       ├── state.md
│       ├── readme.md
│       └── run.sh
├── .gitignore
├── researcher.md
└── coder.md
```
