# 双 Agent 科研协作模板

本仓库使用两个 Agent 协作科研：

- Researcher Agent 负责头脑风暴、阅读单篇论文、分析实验和创建实验。
- Coder Agent 负责初始化仓库、完善工程设计、运行实验和记录事实结果。

每个操作下面都同时提供“本地版”和“SSH 远程版”prompt。SSH 远程版是完整、独立的代码块，可以直接一键复制，不需要拼接公共前缀。创建实验是例外：将后文的简单提示语放进一个完整的 Researcher prompt 后一并发送，不单独使用。Agent 会先读取 `researcher.md` 或 `coder.md` 的行为规范，再结合你填写的补充信息完成本轮操作。每次 prompt 以一个主要操作为主；Researcher 可在你明确要求时，把当前研究上下文中产生的实验按“1.4 创建实验”落地。

Researcher 的所有操作采用“先讨论、后执行”的两阶段模式：可以先做形成讨论方案所必需的只读阅读和证据核对，但任何文件写入、目录创建、Git 暂存或提交，都必须等 Researcher 展示具体方案后，再由你在后续消息中明确确认。最初复制的 prompt 即使已经写明“写回”或“创建”，也只启动讨论，不是讨论后的执行确认；一次明确确认可以覆盖讨论中清楚列出的多项操作。

推荐工作流：

1. Coder 初始化仓库。
2. Researcher 头脑风暴、逐篇阅读论文或分析已有实验，先与你讨论具体方案，经你确认后再写回笔记、分析或按需创建状态为 `s1` 的实验。
3. Coder 完善实验设计，状态变为 `s2`。
4. Coder 在实验目录中通过 `bash run.sh` 运行实验，脚本创建新的 `run_XX`，运行期间为 `s3`，成功后为 `s4`；失败则回到 `s1`。异常退出遗留的 `s3` 通过“查询实验状态”修正。
5. Coder 以编号最大的 `run_XX` 记录事实结果，状态变为 `s5`。
6. Researcher 可在有证据后随时独立分析实验；分析不改变状态。

## 1 使用约定

### 1.1 文件与职责

| 文件或目录 | 用途 |
|---|---|
| `researcher.md` | Researcher 的行为规范，以及运行过程中维护的“2 论文思路” |
| `coder.md` | Coder 的行为规范，以及随 `src/` 代码和已记录实验同步维护的“2 仓库结构” |
| `papers/` | 单篇论文笔记，文件名中的短标题使用英文 |
| `src/` | 完善实验设计时新增或修改的全部 Python 代码 |
| `work_dirs/YYMMDD_HHMM_xxx/` | 一个实验的目录 |
| `state.md` | 当前实验状态，只写一行 `sx 状态说明` |
| `readme.md` | 实验目的、配置、事实结果和独立分析 |
| `run.sh` | 在实验目录中直接执行的零参数入口 |
| `run_XX/` | 一次运行的产物和复现信息 |

除非本次 prompt 对标题格式有特殊要求，新生成的 Markdown 文档一级标题不加结构序号，二级及以下标题使用层级编号，例如 `## 1`、`### 1.1`。更新既有 Markdown 时保留人工结构，只让本次新增标题遵循该规则，不为统一格式整体重排；`state.md` 是只含一行状态且不使用标题的特例。

默认情况下，一个实验根目录的直接子项只能是普通文件 `state.md`、`readme.md`、`run.sh`，以及零个或多个 `run_XX/` 目录，不允许其他文件、目录、隐藏项或符号链接。这个白名单覆盖实验的整个生命周期，但不要求创建时一次备齐：Researcher 创建新实验时只创建 `state.md` 和 `readme.md`；Coder 完善实验设计时可更新 `state.md` 和 `readme.md`，并只把 `run.sh` 作为新增或更新的根级运行入口，不预建 `run_XX`；正式执行 `bash run.sh` 时才创建新的 `run_XX`。发现既有且未经本次 prompt 明确授权的白名单外项目时保留原状并报告，不自动删除、移动或覆盖。

`researcher.md` 的“2 论文思路”和 `coder.md` 的“2 仓库结构”由相应 Agent 在实际项目运行过程中按需维护；本模板不预先改写其中内容。

调用这些科研与实验操作时，Agent 对 `researcher.md` 和 `coder.md` 的“1 行为规范”仅可读取，不得自行修改该节或其下级内容；所有写回仅限对应操作明确允许的文件或章节。需要维护行为规范时，应退出当前操作并另行人工处理。

### 1.2 补充信息

`researcher.md` 和 `coder.md` 的每个具体操作小节都包含执行该操作所需的完整业务规则。README 只提供流程概览；本地 prompt 负责提供项目根目录、指定操作和本次输入，SSH prompt 还会完整包含 SSH 目标、远程作用域与断线安全规则。

项目根目录只在每次复制的 README prompt 中填写：本地版填写本机上的项目绝对路径，SSH 远程版填写远程服务器上的项目绝对路径。`researcher.md` 和 `coder.md` 只负责从本次 prompt 读取并核验路径，不保存具体项目根目录。未填写、不可访问或不是绝对路径时，Agent 会停止并请你修正，不扫描磁盘猜测项目。

每个 prompt 的补充区都没有固定表单。你可以自由填写自然语言、列表、链接、配置、报错或日志，也可以只写一句话。

只有补充信息明确逐项写出实验根目录白名单外直接子项的具体路径、用途和必要性时，才视为你授权本次布局例外；留空或泛泛提到“其他信息”不构成授权。布局例外不改变角色职责、状态门禁或各操作的写入边界。

需要操作既有实验的 prompt 还会单独提供“实验目录”填写位。请在那里明确填写目标实验根目录，例如 `work_dirs/YYMMDD_HHMM_xxx/` 或其绝对路径；Agent 会结合本次 prompt 的项目根目录校验范围，不扫描或猜测目标实验。其余补充信息仍可自由组织。

### 1.3 实验状态与运行目录

实验状态只描述 Coder 的执行流程：

```text
s1 待完善实验设计
s2 待运行实验
s3 实验正在运行
s4 待记录实验结果
s5 实验已完成
```

`state.md` 只包含上面某一行，不保存其他字段。正常流程是 `s1 → s2 → s3 → s4 → s5`；运行失败时由 `s3` 回到 `s1`。Researcher 的实验分析独立于此流程，不新增状态，也不修改 `state.md`。

每次运行尝试都由实验目录中的 `run.sh` 创建新的递增目录：`run_01`、`run_02`……旧目录不复用、不覆盖。编号最大的 `run_XX` 是当前结果来源，前面的目录是失败尝试的信息备份，不与最终结果聚合。一个实验成功后不再重复运行；需要验证新的科学条件时创建新实验。没有本次 prompt 的明确布局例外时，本次运行专属的输入副本、机器可读配置、下载内容、临时与中间文件、缓存、日志、进程与会话信息、退出码、复现信息，以及实际产生且适用的模型权重、checkpoint、推理结果和测试指标，都必须保存在本次新建的 `run_XX` 中，不得散落为实验根目录的其他直接子项。项目共享源码、依赖声明、数据和基础模型仍保留在项目级既有位置，run 中记录可复现引用或按需保存副本。

运行入口固定为：

```bash
bash run.sh
```

不传参数。`run.sh` 自行创建 `run_XX`、维护单行 `state.md`，并把本次运行的工作目录、输出目录、临时目录和适用缓存目录定向到该 `run_XX`。默认情况下，并发控制也不得在实验根目录遗留锁文件、PID 文件或锁目录。

### 1.4 Conda、Git 与忽略目录

具体项目使用的 Conda 环境名，由你手动填写在 `coder.md` 的“1.1.1 Conda相关”。Coder 只使用该名称；仍为 `[TBD]` 时会先请你填写。

对未被 `.gitignore` 忽略的项目文件，Agent 每完成并验证一个逻辑变更就及时提交 Git，只提交本操作产生的内容，默认不 push。Agent 以仓库现有 `.gitignore` 为准，不轻易修改其中的规则；确有必要时只做满足当前任务所需的最小调整，并说明原因和影响。已被忽略的内容保留在本地，不强行提交。

## 2 Researcher Prompt 模板

以下 prompt 一键复制后会启动对应 Researcher 操作的讨论阶段。Researcher 提出具体写入或创建方案后，你还需要在后续消息中明确确认，流程才会进入执行阶段。

### 2.1 头脑风暴

**本地版**

```text
【本地项目根目录（必填，绝对路径）】
在这里填写本机上的项目根目录绝对路径。

请完整读取上述本地项目根目录中的 researcher.md，并严格执行“1.1 头脑风暴”。

【补充信息（格式自由，可留空）】
无。
```

**SSH 远程版**

```text
本次任务只通过 SSH 操作下列远程项目，本地工作区只读。

【SSH 目标（必填）】
在这里填写已有 SSH 配置别名或 user@host。不要填写密码、私钥或 token。

【远程项目根目录（必填，绝对路径）】
在这里填写远程服务器上的项目根目录绝对路径。

使用现有 SSH 认证和主机密钥校验；认证失败、主机密钥未知或不匹配时停止并说明，不关闭或绕过 StrictHostKeyChecking，也不修改全局 SSH 配置。所有项目文件读取、修改、命令和 Git 操作都必须通过 SSH 在远程项目根目录内完成；不得使用本地同名文件作为依据，不得回退到本地执行，未经我明确授权不得使用 scp、rsync 或其他方式传输或同步文件。

连接后先核验实际远程主机和规范化后的远程项目根目录，并确认远程 researcher.md 直接位于该目录根部。若远程目录是 Git 仓库，Git 顶层必须等于该根目录。任一项缺失或不匹配时停止，不猜测或改写路径。以远程 researcher.md 为权威规范，写入前重新读取远程文件并保留已有改动。所有 Git 检查、暂存和提交都只在远程根目录执行，只提交本操作内容，默认不 push；不修改全局 Git 配置，不绕过 hook。

请通过 SSH 完整读取上述远程项目根目录中的 researcher.md，并严格执行“1.1 头脑风暴”。

【补充信息（格式自由，可留空）】
无。

SSH 断开或本地等待超时时，将结果视为未知。重新连接后先核对远程文件和 Git 状态，再决定后续，不盲目重放写命令或 Git 提交。完成后报告实际 SSH 目标、规范化后的远程项目根目录、修改文件和远程 commit（如有）。
```

### 2.2 文献调研

**本地版**

```text
【本地项目根目录（必填，绝对路径）】
在这里填写本机上的项目根目录绝对路径。

请完整读取上述本地项目根目录中的 researcher.md，并严格执行“1.2 文献调研”。

【论文（必填，格式自由）】
在这里填写一篇论文的标题、链接、本地 PDF 路径或其他唯一标识。

【补充信息（格式自由，可留空）】
无。
```

**SSH 远程版**

```text
本次任务只通过 SSH 操作下列远程项目，本地工作区只读。

【SSH 目标（必填）】
在这里填写已有 SSH 配置别名或 user@host。不要填写密码、私钥或 token。

【远程项目根目录（必填，绝对路径）】
在这里填写远程服务器上的项目根目录绝对路径。

使用现有 SSH 认证和主机密钥校验；认证失败、主机密钥未知或不匹配时停止并说明，不关闭或绕过 StrictHostKeyChecking，也不修改全局 SSH 配置。所有项目文件读取、修改、命令和 Git 操作都必须通过 SSH 在远程项目根目录内完成；不得使用本地同名文件作为依据，不得回退到本地执行，未经我明确授权不得使用 scp、rsync 或其他方式传输或同步文件。

连接后先核验实际远程主机和规范化后的远程项目根目录，并确认远程 researcher.md 直接位于该目录根部。若远程目录是 Git 仓库，Git 顶层必须等于该根目录。任一项缺失或不匹配时停止，不猜测或改写路径。以远程 researcher.md 为权威规范，写入前重新读取远程文件并保留已有改动。所有 Git 检查、暂存和提交都只在远程根目录执行，只提交本操作内容，默认不 push；不修改全局 Git 配置，不绕过 hook。

请通过 SSH 完整读取上述远程项目根目录中的 researcher.md，并严格执行“1.2 文献调研”。

【论文（必填，格式自由）】
在这里填写一篇论文的标题、链接、远程服务器 PDF 路径或其他唯一标识。若只有本地文件，先询问我是否允许传输以及远程目标路径，不自行上传。

【补充信息（格式自由，可留空）】
无。

SSH 断开或本地等待超时时，将结果视为未知。重新连接后先核对远程论文笔记和 Git 状态，再决定后续，不盲目重放写命令或 Git 提交。完成后报告实际 SSH 目标、规范化后的远程项目根目录、论文笔记路径和远程 commit（如有）。
```

### 2.3 实验分析

**本地版**

```text
【本地项目根目录（必填，绝对路径）】
在这里填写本机上的项目根目录绝对路径。

请完整读取上述本地项目根目录中的 researcher.md，并严格执行“1.3 实验分析”。

【实验目录（必填）】
在这里填写目标实验根目录路径。

【补充信息（格式自由，可留空）】
无。
```

**SSH 远程版**

```text
本次任务只通过 SSH 操作下列远程项目，本地工作区只读。

【SSH 目标（必填）】
在这里填写已有 SSH 配置别名或 user@host。不要填写密码、私钥或 token。

【远程项目根目录（必填，绝对路径）】
在这里填写远程服务器上的项目根目录绝对路径。

【远程实验目录（必填，绝对路径）】
在这里填写远程服务器上的目标实验根目录绝对路径。

使用现有 SSH 认证和主机密钥校验；认证失败、主机密钥未知或不匹配时停止并说明，不关闭或绕过 StrictHostKeyChecking，也不修改全局 SSH 配置。所有项目文件读取、修改、命令和 Git 操作都必须通过 SSH 在远程项目根目录内完成；不得使用本地同名文件作为依据，不得回退到本地执行，未经我明确授权不得使用 scp、rsync 或其他方式传输或同步文件。

连接后先核验实际远程主机和规范化后的远程项目根目录，并确认远程 researcher.md 直接位于该目录根部。规范化后的远程实验目录必须位于该项目的 work_dirs/ 下，且不是 run_XX 或指向项目外的符号链接。若远程目录是 Git 仓库，Git 顶层必须等于项目根目录。任一项缺失或不匹配时停止，不猜测或改写路径。以远程 researcher.md 为权威规范，写入前重新读取远程实验文件并保留已有改动。所有 Git 检查、暂存和提交都只在远程根目录执行，只提交本操作内容，默认不 push；不修改全局 Git 配置，不绕过 hook。

请通过 SSH 完整读取上述远程项目根目录中的 researcher.md，并严格执行“1.3 实验分析”。

【补充信息（格式自由，可留空）】
无。

SSH 断开或本地等待超时时，将结果视为未知。重新连接后先核对远程实验文件、state.md、run_XX 和 Git 状态，再决定后续，不盲目重放写命令或 Git 提交。完成后报告实际 SSH 目标、规范化后的远程项目根目录、远程实验目录、所用 run、修改文件和远程 commit（如有）。
```

创建实验只表示新建实验，不用于修改已有实验。需要创建新实验时，不使用单独模板；把对应提示放进一个完整的 Researcher 本地版或 SSH 远程版 prompt 后一并发送，项目根目录及 SSH 上下文继承自主 prompt。附加提示只启动实验方案讨论，不授权立即创建；Researcher 提出确切方案后，你需要在后续消息中明确确认创建。若附加在实验分析 prompt 中，其中的实验目录仍只表示待分析的已有实验；新实验由“1.4 创建实验”另建目录。

**本地版附加提示**

```text
请先与我讨论并提出 researcher.md“1.4 创建实验”的完整方案；待我在讨论后明确确认，再在上述本地项目根目录中创建新实验。
```

**SSH 远程版附加提示**

```text
请先与我讨论并提出远程 researcher.md“1.4 创建实验”的完整方案；待我在讨论后明确确认，再通过上述 SSH 连接在上述远程项目根目录中创建新实验。
```

## 3 Coder Prompt 模板

### 3.1 仓库初始化

**本地版**

```text
【本地项目根目录（必填，绝对路径）】
在这里填写本机上的项目根目录绝对路径。

请完整读取上述本地项目根目录中的 coder.md，并严格执行“1.1 仓库初始化”。

【补充信息（格式自由，可留空）】
无。
```

**SSH 远程版**

```text
本次任务只通过 SSH 初始化或接管下列已放好模板的远程项目，本地工作区只读。本操作不从空目录引导或传输模板。

【SSH 目标（必填）】
在这里填写已有 SSH 配置别名或 user@host。不要填写密码、私钥或 token。

【远程项目根目录（必填，绝对路径）】
在这里填写远程服务器上的项目根目录绝对路径。

使用现有 SSH 认证和主机密钥校验；认证失败、主机密钥未知或不匹配时停止并说明，不关闭或绕过 StrictHostKeyChecking，也不修改全局 SSH 配置。所有项目文件读取、修改、命令和 Git 操作都必须通过 SSH 在远程项目根目录内完成；不得使用本地同名文件作为依据，不得回退到本地执行，未经我明确授权不得使用 scp、rsync 或其他方式传输或同步文件。

连接后先核验实际远程主机和规范化后的远程项目根目录，并确认远程 coder.md 直接位于该目录根部。目标目录或 coder.md 不存在时停止，不创建目录或复制模板。若远程目录已经是 Git 仓库，Git 顶层必须等于该根目录。任一项不匹配时停止，不猜测或改写路径。以远程 coder.md 为权威规范，写入前重新读取远程文件并保留已有改动。所有 Git 检查、暂存和提交都只在远程根目录执行，只提交本操作内容，默认不 push；不修改全局 Git 配置，不绕过 hook。

请通过 SSH 完整读取上述远程项目根目录中的 coder.md，并严格执行“1.1 仓库初始化”。

【补充信息（格式自由，可留空）】
无。

SSH 断开或本地等待超时时，将结果视为未知。重新连接后先核对远程文件、环境和 Git 状态，再决定后续，不盲目重放写命令或 Git 提交。完成后报告实际 SSH 目标、规范化后的远程项目根目录、修改文件、验证结果和远程 commit（如有）。
```

### 3.2 查询实验状态

**本地版**

```text
【本地项目根目录（必填，绝对路径）】
在这里填写本机上的项目根目录绝对路径。

请完整读取上述本地项目根目录中的 coder.md，并严格执行“1.2.0 查询实验状态”。

【实验目录（必填）】
在这里填写目标实验根目录路径。

【补充信息（格式自由，可留空）】
无。
```

**SSH 远程版**

```text
本次任务只通过 SSH 操作下列远程项目，本地工作区只读。

【SSH 目标（必填）】
在这里填写已有 SSH 配置别名或 user@host。不要填写密码、私钥或 token。

【远程项目根目录（必填，绝对路径）】
在这里填写远程服务器上的项目根目录绝对路径。

【远程实验目录（必填，绝对路径）】
在这里填写远程服务器上的目标实验根目录绝对路径。

使用现有 SSH 认证和主机密钥校验；认证失败、主机密钥未知或不匹配时停止并说明，不关闭或绕过 StrictHostKeyChecking，也不修改全局 SSH 配置。所有项目文件读取、修改、命令和 Git 操作都必须通过 SSH 在远程项目根目录内完成；不得使用本地同名文件作为依据，不得回退到本地执行，未经我明确授权不得使用 scp、rsync 或其他方式传输或同步文件。

连接后先核验实际远程主机和规范化后的远程项目根目录，并确认远程 coder.md 直接位于该目录根部。规范化后的远程实验目录必须位于该项目的 work_dirs/ 下，且不是 run_XX 或指向项目外的符号链接。若远程目录是 Git 仓库，Git 顶层必须等于项目根目录。任一项缺失或不匹配时停止，不猜测或改写路径。以远程 coder.md 为权威规范，操作前重新读取远程实验文件并保留已有内容。所有 Git 检查、暂存和提交都只在远程根目录执行，只提交本操作内容，默认不 push；不修改全局 Git 配置，不绕过 hook。

请通过 SSH 完整读取上述远程项目根目录中的 coder.md，并严格执行“1.2.0 查询实验状态”。

【补充信息（格式自由，可留空）】
无。

SSH 断开或本地等待超时时，将结果视为未知。重新连接后先核对远程 tmux、进程、state.md、最大 run_XX、日志和 Git 状态，再决定后续，不盲目重放写命令或 Git 提交。完成后报告实际 SSH 目标、规范化后的远程项目根目录、远程实验目录、状态与证据、状态变化和远程 commit（如有）。
```

### 3.3 完善实验设计

**本地版**

```text
【本地项目根目录（必填，绝对路径）】
在这里填写本机上的项目根目录绝对路径。

请完整读取上述本地项目根目录中的 coder.md，并严格执行“1.2.1 完善实验设计”。

【实验目录（必填）】
在这里填写目标实验根目录路径。

【补充信息（格式自由，可留空）】
无。
```

**SSH 远程版**

```text
本次任务只通过 SSH 操作下列远程项目，本地工作区只读。

【SSH 目标（必填）】
在这里填写已有 SSH 配置别名或 user@host。不要填写密码、私钥或 token。

【远程项目根目录（必填，绝对路径）】
在这里填写远程服务器上的项目根目录绝对路径。

【远程实验目录（必填，绝对路径）】
在这里填写远程服务器上的目标实验根目录绝对路径。

使用现有 SSH 认证和主机密钥校验；认证失败、主机密钥未知或不匹配时停止并说明，不关闭或绕过 StrictHostKeyChecking，也不修改全局 SSH 配置。所有项目文件读取、修改、命令和 Git 操作都必须通过 SSH 在远程项目根目录内完成；不得使用本地同名文件作为依据，不得回退到本地执行，未经我明确授权不得使用 scp、rsync 或其他方式传输或同步文件。

连接后先核验实际远程主机和规范化后的远程项目根目录，并确认远程 coder.md 直接位于该目录根部。规范化后的远程实验目录必须位于该项目的 work_dirs/ 下，且不是 run_XX 或指向项目外的符号链接。若远程目录是 Git 仓库，Git 顶层必须等于项目根目录。任一项缺失或不匹配时停止，不猜测或改写路径。以远程 coder.md 为权威规范，写入前重新读取远程实验文件、源码和 Git 状态并保留已有改动。所有 Git 检查、暂存和提交都只在远程根目录执行，只提交本操作内容，默认不 push；不修改全局 Git 配置，不绕过 hook。

请通过 SSH 完整读取上述远程项目根目录中的 coder.md，并严格执行“1.2.1 完善实验设计”。

【补充信息（格式自由，可留空）】
无。

SSH 断开或本地等待超时时，将结果视为未知。重新连接后先核对远程源码、实验文件、state.md、run_XX 和 Git 状态，再决定后续，不盲目重放写命令或 Git 提交。完成后报告实际 SSH 目标、规范化后的远程项目根目录、远程实验目录、修改文件、状态变化、验证结果和远程 commit（如有）。
```

### 3.4 运行实验

**本地版**

```text
【本地项目根目录（必填，绝对路径）】
在这里填写本机上的项目根目录绝对路径。

请完整读取上述本地项目根目录中的 coder.md，并严格执行“1.2.2 运行实验”。

【实验目录（必填）】
在这里填写目标实验根目录路径。

【补充信息（格式自由，可留空）】
无。
```

**SSH 远程版**

```text
本次任务只通过 SSH 操作下列远程项目，本地工作区只读。

【SSH 目标（必填）】
在这里填写已有 SSH 配置别名或 user@host。不要填写密码、私钥或 token。

【远程项目根目录（必填，绝对路径）】
在这里填写远程服务器上的项目根目录绝对路径。

【远程实验目录（必填，绝对路径）】
在这里填写远程服务器上的目标实验根目录绝对路径。

使用现有 SSH 认证和主机密钥校验；认证失败、主机密钥未知或不匹配时停止并说明，不关闭或绕过 StrictHostKeyChecking，也不修改全局 SSH 配置。所有项目文件读取、修改、命令和 Git 操作都必须通过 SSH 在远程项目根目录内完成；不得使用本地同名文件作为依据，不得回退到本地执行，未经我明确授权不得使用 scp、rsync 或其他方式传输或同步文件。

连接后先核验实际远程主机和规范化后的远程项目根目录，并确认远程 coder.md 直接位于该目录根部。规范化后的远程实验目录必须位于该项目的 work_dirs/ 下，且不是 run_XX 或指向项目外的符号链接。若远程目录是 Git 仓库，Git 顶层必须等于项目根目录。任一项缺失或不匹配时停止，不猜测或改写路径。以远程 coder.md 为权威规范，启动前重新读取远程实验文件、源码和 Git 状态并保留已有内容。所有 Git 检查都只在远程根目录执行；不得修改本地 Git，默认不 push，不修改全局 Git 配置，不绕过 hook。

请通过 SSH 完整读取上述远程项目根目录中的 coder.md，并严格执行“1.2.2 运行实验”。

【补充信息（格式自由，可留空）】
无。

SSH 断开或本地等待超时时，将运行结果视为未知，不得再次执行 bash run.sh。重新连接后先核对远程 tmux、进程、state.md、最大 run_XX、日志、退出码、必要产物和 Git 状态，再决定后续。完成后报告实际 SSH 目标、规范化后的远程项目根目录、远程实验目录、tmux 会话、进程、最大 run、状态和证据路径。
```

### 3.5 记录实验结果

**本地版**

```text
【本地项目根目录（必填，绝对路径）】
在这里填写本机上的项目根目录绝对路径。

请完整读取上述本地项目根目录中的 coder.md，并严格执行“1.2.3 记录实验结果”。

【实验目录（必填）】
在这里填写目标实验根目录路径。

【补充信息（格式自由，可留空）】
无。
```

**SSH 远程版**

```text
本次任务只通过 SSH 操作下列远程项目，本地工作区只读。

【SSH 目标（必填）】
在这里填写已有 SSH 配置别名或 user@host。不要填写密码、私钥或 token。

【远程项目根目录（必填，绝对路径）】
在这里填写远程服务器上的项目根目录绝对路径。

【远程实验目录（必填，绝对路径）】
在这里填写远程服务器上的目标实验根目录绝对路径。

使用现有 SSH 认证和主机密钥校验；认证失败、主机密钥未知或不匹配时停止并说明，不关闭或绕过 StrictHostKeyChecking，也不修改全局 SSH 配置。所有项目文件读取、修改、命令和 Git 操作都必须通过 SSH 在远程项目根目录内完成；不得使用本地同名文件作为依据，不得回退到本地执行，未经我明确授权不得使用 scp、rsync 或其他方式传输或同步文件。

连接后先核验实际远程主机和规范化后的远程项目根目录，并确认远程 coder.md 直接位于该目录根部。规范化后的远程实验目录必须位于该项目的 work_dirs/ 下，且不是 run_XX 或指向项目外的符号链接。若远程目录是 Git 仓库，Git 顶层必须等于项目根目录。任一项缺失或不匹配时停止，不猜测或改写路径。以远程 coder.md 为权威规范，写入前重新读取远程实验文件、最大 run_XX 和 Git 状态并保留已有改动。所有 Git 检查、暂存和提交都只在远程根目录执行，只提交本操作内容，默认不 push；不修改全局 Git 配置，不绕过 hook。

请通过 SSH 完整读取上述远程项目根目录中的 coder.md，并严格执行“1.2.3 记录实验结果”。

【补充信息（格式自由，可留空）】
无。

SSH 断开或本地等待超时时，将结果视为未知。重新连接后先核对远程 readme.md、state.md、最大 run_XX、coder.md 和 Git 状态，再决定后续，不盲目重放写命令或 Git 提交。完成后报告实际 SSH 目标、规范化后的远程项目根目录、远程实验目录、所用 run、状态变化、修改文件和远程 commit（如有）。
```
